import 'package:flutter/material.dart';

class Rekomendation extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) => Scaffold(
    backgroundColor: Colors.red,
    body: Center(
      child: Text('Rekomendation', style: TextStyle(fontSize: 60),),
    ),
  );
}